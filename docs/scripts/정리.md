# playwirght

## 특정 파일
npx playwright test e2e/charts-page.spec.ts

## 특정 테스트 이름 (grep)
npx playwright test -g "날짜 선택"

## 브라우저 띄워서 보기 (headed)
npx playwright test e2e/charts-page.spec.ts --headed

## 디버그 모드 (step-by-step)
npx playwright test e2e/charts-page.spec.ts --debug

## 특정 test 실행, glob 텍스트로 포함
npx playwright test e2e/charts-page.spec.ts -g "데이터 로드"